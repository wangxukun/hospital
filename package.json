{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.11.30",
    "lerna": "^8.1.2",
    "typescript": "^5.4.2"
  },
 "scripts": {
   "publish": "lerna run tsc && lerna publish"
 }
}
